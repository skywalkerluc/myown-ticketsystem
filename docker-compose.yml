version: "3"
services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    environment:
      NODE_ENV: production
    depends_on:
      - redis

  redis:
    image: "redis:latest"
    ports:
      - "6379:6379"
